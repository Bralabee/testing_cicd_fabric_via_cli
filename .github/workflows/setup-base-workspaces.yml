# ─────────────────────────────────────────────────────────────────────────────
# Initial Environment Setup — GitHub Actions Workflow
#
# Run ONCE (manually) to provision the base Dev/Test/Prod workspaces and
# connect them to a Fabric Deployment Pipeline. This is the STARTING POINT
# before any feature branch work begins.
#
# After this workflow runs successfully:
#   - Dev workspace exists and is Git-synced to `main`
#   - Test and Prod workspaces exist (content promoted via Deployment Pipeline)
#   - Feature branches can now create isolated workspaces
#
# Usage: Actions → "Setup Base Workspaces" → Run workflow
# ─────────────────────────────────────────────────────────────────────────────

name: Setup Base Workspaces

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy (dev deploys the main Dev workspace)'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev

jobs:
  setup-base-workspaces:
    name: Deploy Base Dev Workspace
    runs-on: ubuntu-latest

    env:
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      GITHUB_TOKEN_FABRIC: ${{ secrets.FABRIC_GITHUB_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.FABRIC_GITHUB_TOKEN }}
      FABRIC_CAPACITY_ID: ${{ secrets.FABRIC_CAPACITY_ID }}
      DEV_ADMIN_OBJECT_ID: ${{ secrets.DEV_ADMIN_OBJECT_ID }}
      GIT_REPO_URL: ${{ github.server_url }}/${{ github.repository }}
      PROJECT_PREFIX: ${{ vars.PROJECT_PREFIX || 'fabric-cicd-demo' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install CLI tool
        run: |
          pip install --upgrade pip
          pip install ms-fabric-cli==${{ vars.FABRIC_CLI_VERSION || '1.3.1' }}
          pip install "git+https://${{ secrets.FABRIC_GITHUB_TOKEN }}@${{ vars.CLI_REPO_URL || 'github.com/BralaBee-LEIT/usf_fabric_cli_cicd_codebase' }}.git@${{ vars.CLI_REPO_REF || 'v1.7.7' }}"

      - name: Verify credentials
        run: |
          for var in AZURE_TENANT_ID AZURE_CLIENT_ID AZURE_CLIENT_SECRET FABRIC_CAPACITY_ID GITHUB_TOKEN_FABRIC DEV_ADMIN_OBJECT_ID; do
            if [ -z "${!var}" ]; then
              echo "::error::Required secret $var is not configured"
              exit 1
            fi
          done
          echo "All required credentials verified"

      - name: Deploy Dev workspace
        run: |
          echo "📦 Deploying base Dev workspace..."
          fabric-cicd deploy \
            config/projects/demo/base_workspace.yaml \
            --env ${{ inputs.environment }} \
            --rollback-on-failure

      - name: Report setup complete
        if: success()
        run: |
          echo "## ✅ Base Workspace Setup Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Property | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|:---|:---|" >> $GITHUB_STEP_SUMMARY
          echo "| **Environment** | \`${{ inputs.environment }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Config** | \`config/projects/demo/base_workspace.yaml\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Triggered by** | ${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### What was deployed" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Dev workspace created and Git-synced to \`main\`" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Deployment Pipeline created (configured in base_workspace.yaml)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Test and Prod workspaces created and assigned to pipeline" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Next Steps" >> $GITHUB_STEP_SUMMARY
          echo "1. Create a \`feature/*\` branch to test the feature workspace lifecycle" >> $GITHUB_STEP_SUMMARY
          echo "2. Merge a PR to \`main\` to trigger automatic Dev → Test promotion" >> $GITHUB_STEP_SUMMARY
          echo "3. Manually trigger **Promote Test → Prod** workflow when ready" >> $GITHUB_STEP_SUMMARY

      - name: Report failure
        if: failure()
        run: |
          echo "## ❌ Base Workspace Setup Failed" >> $GITHUB_STEP_SUMMARY
          echo "Check the logs above for details." >> $GITHUB_STEP_SUMMARY
